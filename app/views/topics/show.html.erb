<div class="container">
  <div class="row">
    <% if @topic %>
    <h3>
      <%= link_to @topic, data: { confirm: 'Are you sure you want to delete this topic?' }, method: :delete, class: 'btn btn-danger btn-xs' do %>
      <span class="has-tooltip" data-toggle="tooltip" title="Delete Topic">
        <span class="glyphicon glyphicon-trash">
        </span>
      </span>
      <% end %>
      <%= link_to edit_topic_path(@topic), class: 'btn btn-success btn-xs' do %>
      <span class="has-tooltip" data-toggle="tooltip" title="Edit Topic">
        <span class="glyphicon glyphicon-pencil">
        </span>
      </span>
      <% end %>
      Topic: <%= @topic.title %></h3>
      <h4><%= "Bookmarks:" if @bookmarks.present? %></h4>
      <%= render partial: 'bookmarks/bookmark', collection: @bookmarks, locals: { topic_id: @topic.id } %>
      <%= render partial: 'bookmarks/form', locals: { topic_id: @topic.id } %>
      <% else %>
      <%= link_to "New Topic", new_topic_path, class: 'btn btn-warning' %>
      <% end %>
      <br />
    </div>
  </div>
